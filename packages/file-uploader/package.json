{
  "name": "react-background-file-uploader",
  "version": "0.0.4",
  "description": "A powerful file uploader with resumable support",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
"scripts": {
  "build": "tsup src/index.ts --dts",
  "dev": "tsup src/index.ts --watch --dts",
  "test": "vitest",
  "prepare": "pnpm build"
},
  "dependencies": {
    "zustand": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "tsup": "^7.2.0",
    "vitest": "^1.2.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
